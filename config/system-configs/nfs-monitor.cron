# NFS Health Monitor - runs every 5 minutes
# Detects and automatically recovers from stale NFS mounts
*/5 * * * * /home/cyl/homelab/scripts/monitor-nfs-health.sh check >> /var/log/nfs-health-monitor.log 2>&1
