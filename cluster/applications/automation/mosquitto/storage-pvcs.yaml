---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-data
  namespace: automation
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-direct
  volumeName: mosquitto-data
  resources:
    requests:
      storage: 1Gi
