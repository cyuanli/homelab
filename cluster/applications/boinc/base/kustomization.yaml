apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: boinc

resources:
  - namespace.yaml
  - configmap.yaml
  - service.yaml
  - daemonset.yaml

# Secrets should be created separately (see secrets.yaml.template)
