# Homelab Configuration Template for cyl-optiplex9020
# Copy to config.env.local and customize with your secrets

# =======================
# BASIC CONFIGURATION
# =======================

# Domain and networking
DOMAIN="cliff.li"
ACME_EMAIL="cyl@cliff.li"

# User configuration
HOMELAB_USER="cyl"
HOMELAB_UID="1000"
HOMELAB_GID="1000"

# =======================
# K3S CONFIGURATION
# =======================

# Node role and cluster info
NODE_ROLE=server
CLUSTER_TOKEN=REPLACE_WITH_CLUSTER_TOKEN
SERVER_URL=REPLACE_WITH_SERVER_URL

# K3s version
K3S_VERSION="v1.33.5+k3s1"
KUBECTL_VERSION="v1.33.5"

# =======================
# TAILSCALE CONFIGURATION
# =======================

# Tailscale authentication key
TAILSCALE_AUTHKEY=REPLACE_WITH_YOUR_TAILSCALE_AUTHKEY

# =======================
# STORAGE CONFIGURATION
# =======================

# Storage configuration (inherited from main cluster)
DATA_ROOT="/media/data"
K8S_STORAGE_ROOT="/opt/k3s-storage"

# =======================
# SERVICE CONFIGURATION
# =======================

# Container user/group IDs
PUID="1000"
PGID="1000"
TIMEZONE="Etc/UTC"

# Feature flags
ENABLE_TRAEFIK_DASHBOARD="false"
ENABLE_LOCATION_SERVICES="false"
ENABLE_DISK_MONITORING="true"
ENABLE_BACKUP_MONITORING="true"

# =======================
# MONITORING & NOTIFICATIONS
# =======================


# =======================
# KUBELET CONFIGURATION
# =======================

# Resource reservations for system and Kubernetes components
# This node has 4 CPUs and 8GB RAM
# Restricted to essential workloads only (monitoring, infrastructure)
# CPU: Reserving 1 core (500m system + 500m K8s) out of 4 cores via CPU pinning
#      CPU 0: Reserved for system (kubelet, containerd, OS)
#      CPUs 1-3: Available for pods (3 CPUs allocatable)
# Memory: Reserving 2GB total (1.5GB system + 500Mi K8s) out of 8GB
#         Leaves ~6GB allocatable for workloads
KUBELET_SYSTEM_RESERVED_CPU="500m"
KUBELET_SYSTEM_RESERVED_MEMORY="1.5Gi"
KUBELET_KUBE_RESERVED_CPU="500m"
KUBELET_KUBE_RESERVED_MEMORY="500Mi"

# CPU Manager Policy: static enables CPU pinning for Guaranteed QoS pods
# Reserved CPUs (system-reserved + kube-reserved = 1) are never allocated to pods
KUBELET_CPU_MANAGER_POLICY="static"

# =======================
# ADVANCED CONFIGURATION
# =======================

# UFW firewall configuration
ENABLE_UFW="true"
ALLOW_SSH_FROM_LAN="true"
LAN_CIDR="192.168.0.0/16"
